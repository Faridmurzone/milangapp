import 'package:flutter/material.dart';

class ProfileMilas extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
        color: Colors.redAccent
    );
    throw UnimplementedError();
  }
}